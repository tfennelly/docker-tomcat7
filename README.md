# jenkinsci/tomcat7 Docker image

Build:

```
docker build -t jenkinsci/tomcat7 .
```

Sample `Dockerfile` that builds from it: [tfennelly/spring-petclinic](https://github.com/tfennelly/spring-petclinic/blob/master/Dockerfile)
